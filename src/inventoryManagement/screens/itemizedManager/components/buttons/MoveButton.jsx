import { Button } from "@mui/material";
import "./Button.css";

function MoveButton() {
  return (
    <Button variant="contained" className="move">
      Move
    </Button>
  );
}

export default MoveButton;

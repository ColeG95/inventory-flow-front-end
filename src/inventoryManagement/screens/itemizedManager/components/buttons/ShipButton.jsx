import { Button } from "@mui/material";
import * as color from "@mui/material/colors";
import "./Button.css";

function ShipButton() {
  return (
    <Button variant="contained" className="ship">
      Ship
    </Button>
  );
}

export default ShipButton;
